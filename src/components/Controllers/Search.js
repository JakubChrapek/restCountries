import React from "react"

const Search = () => {
  return (
    <input
      name="search"
      name="search"
      id="search"
      placeholder="Search for a country..."
    />
  )
}

export default Search
