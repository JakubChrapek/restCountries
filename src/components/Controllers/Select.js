import React from "react"

const Select = () => {
  return (
    <input
      type="select"
      name="Select"
      name="Select"
      id="Select"
      placeholder="Filter by region"
    />
  )
}

export default Select
